export type EditAboutExperienceState = {
  error: string | null;
  success: boolean;
};

export const initialEditAboutExperienceState: EditAboutExperienceState = {
  error: null,
  success: false,
};
