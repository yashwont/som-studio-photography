export type PortfolioCoverBlockState = {
  error: string | null;
  success: string | null;
};

export const initialPortfolioCoverBlockState: PortfolioCoverBlockState = {
  error: null,
  success: null,
};
